# Agent Instructions

Project-wide coding rules and conventions are in `.kilo/instructions/`.
Read the relevant file before making changes.

## Default branch

The canonical default branch is `master`. All feature branches should be cut from `master`, and all PRs should be raised against `master`.

A `main` branch also exists as a mirror, but do not use it for new work.
